#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."
echo ""

# Run lint-staged which will handle:
# - ESLint with auto-fix
# - Prettier formatting
# - TypeScript type checking
npm run pre-commit

echo ""
echo "✅ Pre-commit checks passed!"
